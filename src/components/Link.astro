---
interface Props {
    href: string;
    label: string;
}

const { href, label } = Astro.props;
---

<a
    href={href}
    class="font-semibold text-muted-foreground hover:text-primary text-sm transition-all duration-300 link-underline"
>
    {label}
</a>
